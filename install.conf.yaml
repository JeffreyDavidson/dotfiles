- defaults:
    link:
      create: true
      force: true
    shell:
      stdout: true
      stderr: true

- clean: ["~"]

- shell:
    - [git submodule update --init --recursive, Updating submodules]
