# wget configuration
# Store HSTS database in XDG state directory
hsts-file = ~/.local/state/wget/hsts

# Other useful wget defaults
continue = on
progress = bar
timestamping = on
no_parent = on
timeout = 60
tries = 3