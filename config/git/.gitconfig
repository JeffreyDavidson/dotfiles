[user]
	name = Jeffrey Davidson
	email = 3826104+JeffreyDavidson@users.noreply.github.com
    username = jeffreydavidson
[init]
	defaultBranch = main
[core]
    pager = delta
    excludesfile = /Users/jeffreydavidson/.config/git/.gitignore_global
[web]
	browser = firefox
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true    # use n and N to move between diff sections
    side-by-side = true
    line-numbers = true

    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
